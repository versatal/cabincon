import { Meteor } from 'meteor/meteor';

import '../imports/api/games.js';
import '../imports/api/posts.js';
import '../imports/api/usereData';
import '../imports/api/blogs';
import '../imports/api/comments';

Meteor.startup(() => {
  // code to run on server at startup
});
